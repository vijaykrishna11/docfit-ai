package com.docfitai.backend.report.dto;

/** Minimal acknowledgement -- never echoes back a "this will definitely be fixed" promise (CLAUDE.md "Report UI"). */
public record ReportSubmittedDto(Long id) {
}
